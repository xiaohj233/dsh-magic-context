{
  "name": "@xiao_hj909/magic-context-for-dsh-adapter",
  "version": "0.1.0",
  "type": "module",
  "description": "Magic Context adapter-api: stable Host-neutral facade over the shared Magic Context core for harness adapters (initial: type + contract surface only, per PLAN §2).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "bun build src/index.ts --outdir dist --target node --format esm",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "@types/node": "^22.20.0",
    "typescript": "^5.8.0"
  }
}
